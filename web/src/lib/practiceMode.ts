/**
 * Practice-set mode is intentionally disabled until Start practice / list entry ship.
 * Keep store + custom favorites order; do not surface banners or `?set=practice` behavior.
 */
export const PRACTICE_MODE_ENABLED = false
